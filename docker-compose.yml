version: '3.9'
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: beeshop
      POSTGRES_USER: beeuser
      POSTGRES_PASSWORD: bee123
    ports: ['5433:5432']
    volumes: [beeshop_data:/var/lib/postgresql/data]
volumes: { beeshop_data: {} }
